# Product Event Consumer Service

## Run service(Follow Sequence)
1. rename ".example.env" file to ".env" [this loads the values into docker]
```shell
docker-compose build
```
```shell
docker-compose up
```
